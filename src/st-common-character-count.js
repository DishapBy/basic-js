export default function getCommonCharacterCount(a, b) {
    // let result = 0;

    // for(let i = 0; i < a.length; i++){
    //     if(b.indexOf(a[i])){
    //         result++;
    //     }
    // }
}